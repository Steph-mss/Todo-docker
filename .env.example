# ============================================
# CONFIGURATION POSTGRESQL
# ============================================

# Hôte de la base de données (nom du service Docker)
DB_HOST=db

# Utilisateur PostgreSQL
DB_USER=postgres

# ⚠️ IMPORTANT - SÉCURITÉ ⚠️
# Changez OBLIGATOIREMENT ce mot de passe !
# Utilisez un mot de passe fort (min 16 caractères, lettres, chiffres, symboles)
# Exemple de génération: openssl rand -base64 32
DB_PASSWORD=CHANGEZ_MOI_OBLIGATOIREMENT

# Nom de la base de données
DB_NAME=tododb

# Port PostgreSQL (interne au réseau Docker)
DB_PORT=5432

# ============================================
# CONFIGURATION BACKEND
# ============================================

# Environnement d'exécution (development, production)
NODE_ENV=production

# Port de l'API (interne)
PORT=3000

# Origines CORS autorisées (séparées par des virgules)
CORS_ORIGIN=http://localhost:8080

# ============================================
# CONFIGURATION FRONTEND
# ============================================

# URL de l'API backend (pour le build Vite)
# En développement local: http://localhost:3000
# En production: URL de votre API (ex: https://api.votredomaine.com)
VITE_API_URL=http://localhost:3000

# ============================================
# NOTES DE SÉCURITÉ
# ============================================
# 1. Ne JAMAIS commiter le fichier .env dans Git
# 2. Utiliser des mots de passe différents pour chaque environnement
# 3. En production, utiliser des secrets managers (AWS Secrets Manager, Azure Key Vault, etc.)
# 4. Restreindre l'accès au fichier .env (chmod 600 sur Linux/macOS)
